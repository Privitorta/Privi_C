#include <stdio.h>

int main(){
    char str[] = "Pippo"; // array di char con la costante "Pippo"
    char c = 'Z'; // variabile char con la costante carattere 'Z'

    printf("il contenuto della stringa str è: %s\n", str);
    printf("il contenuto della stringa c è: %c\n", c);

    return 0;
}